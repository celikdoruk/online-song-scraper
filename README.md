# Online Song Scraper
A python web scraper that scrapes pure HTML's from songbpm.com and stores the data in a PostgreSQL database.
### To Run:
1 - install the requirements with pip install -r requirements.txt
2 - run python3 main.py
3 - additionally you can modify the url_list.py file with the url's you would like to scrape.
